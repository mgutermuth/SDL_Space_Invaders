in the Visual Studio 9.0 folder, go to VC. copy the content of lib into the VC copy of lib. In the include folder, copy the entire folder called SDL into include. If you're on a 64bit computer, dont put the files from system32 into your system32 folder, put them in SysWOW64.

Ta-da